import React from 'react';
import { AboutSection, HomeSection, ConnectSection } from '../../../module';
export default function page() {
	return (
		<main>
			<HomeSection />
			<AboutSection />
			<ConnectSection />
		</main>
	);
}
